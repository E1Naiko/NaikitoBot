[pytest]
asyncio_mode = auto
# La capa de datos usa un motor asíncrono cuyas conexiones quedan ligadas al
# event loop donde se crearon: todas las pruebas comparten un único loop de
# sesión para poder reutilizarlo.
asyncio_default_test_loop_scope = session
asyncio_default_fixture_loop_scope = session
